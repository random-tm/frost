#!/bin/bash
#Wrapper for pacman and AUR helper; intended for use with a build box

if ((EUID == 0)); then
  echo "Do not run as root"
  exit 1
fi

case $1 in
	-search)
		sudo pacman -Ss "$2"
		;;
	-upgrade)
		sudo pacman -Sc --noconfirm
		sudo pacman -Syu
		cd /tmp
		cat /opt/frost | while read line;
		do
			VERSION=$(wget -O- -q http://192.168.1.30:8000/$line.version)
			CURRENT_VERSION=$(cat /opt/frost-versions/$line)
			if [[ "$VERSION" !=  "$CURRENT_VERSION" ]]; then
				wget http://192.168.1.30:8000/$line.pkg.tar.zst
				sudo pacman -U --noconfirm $line.pkg.tar.zst
                        	rm *.pkg.tar.zst
				echo $VERSION > /opt/frost-versions/$line
			fi
		done
		;;
	-clean)
		sudo pacman -Sc
		sudo pacman -Rns $(pacman -Qtdq)
		;;
	-install)
		sudo pacman -S "$2"
		;;
	-remove)
		sudo pacman -Rsn "$2"
		;;
	-mirror-change)
		sudo nano /etc/pacman.d/mirrorlist.pacnew
		sudo rm /etc/pacman.d/mirrorlist
		sudo mv /etc/pacman.d/mirrorlist.pacnew /etc/pacman.d/mirrorlist
		;;
	*)
		echo "-upgrade, -clean, -install (package name), -mirror-change, -remove (package name), -search (search term)"
esac
